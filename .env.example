# 通用 OpenAI 兼容配置（复制为 .env 后填入真实值，.env 已被 .gitignore 忽略，勿提交）
# 变量名固定（OPENAI_API_KEY / OPENAI_BASE_URL / OPENAI_MODEL），切换服务商只需改「值」，不用改变量名。
# AMD Key 获取：https://developer.amd.com.cn/radeon/tokenfactory （登录后在免费模型卡片详情里复制 rc- 开头的 Key）

# 必填：任意 OpenAI 兼容服务的 API Key（AMD 的以 rc- 开头；也兼容 sk- 等其它前缀）
OPENAI_API_KEY=rc-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# 选填：OpenAI 兼容端点。默认 AMD Radeon Cloud；换服务商改这一行的值即可。
OPENAI_BASE_URL=https://developer.amd.com.cn/radeon/api/v1

# 选填：模型 ID。默认 AMD 免费公共文本模型 DeepSeek-V4-Flash-0731（1M 上下文、支持 tool calling）。
# 其它可选：Qwen3.8-Flash-Next（免费多模态）、MiniCPM5-1B / MiniCPM5-2B（免费轻量）、
#          Qwen3.8-27B（Dedicated，消耗 credits，并发/稳定性更好）。
OPENAI_MODEL=DeepSeek-V4-Flash

# 选填：传给模型的额外参数（JSON 字符串）。AMD 免费 Flash 模型默认无思考模式，故默认空 {}。
# 若用 Qwen 系列且想关闭思考可改为 {"enable_thinking": false}（仅 Qwen3.x 识别，其它模型可能忽略或报错）。
EXTRA_BODY={}

# 选填：生成长度上限
MAX_TOKENS=64
